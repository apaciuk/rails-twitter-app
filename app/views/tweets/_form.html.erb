<%= form_with(model: tweet, local: true) do |form| %>
  <% if tweet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tweet.errors.count, "error") %> prohibited this twonk from being saved:</h2>

      <ul>
      <% tweet.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="mb-3">
    <%= form.label :tweet, class: 'form-label' %>
    <%= form.text_area :tweet, class: 'form-control' %>
  </div>

  <div class="mb-3">
    <% if tweet.persisted? %>
      <div class="float-right">
        <%= link_to 'Destroy', tweet, method: :delete, class: "text-danger", data: { confirm: 'Are you sure?' } %>
      </div>
    <% end %>

    <%= form.submit class: 'btn btn-primary' %>

    <% if tweet.persisted? %>
      <%= link_to "Cancel", tweet, class: "btn btn-link" %>
    <% else %>
      <%= link_to "Cancel", tweets_path, class: "btn btn-link" %>
    <% end %>
  </div>
<% end %>
